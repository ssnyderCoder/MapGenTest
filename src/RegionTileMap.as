package  
{
	import net.flashpunk.graphics.Tilemap;
	/**
	 * ...
	 * @author Sean Snyder
	 */
	internal final class RegionTileMap 
	{
		public var tilemap:Tilemap = new Tilemap(Assets.BLOCKS, Map.TILE_LENGTH * Map.BLOCK_LENGTH, Map.TILE_LENGTH * Map.BLOCK_LENGTH, Map.BLOCK_LENGTH, Map.BLOCK_LENGTH);
		public var xRegion:int = int.MIN_VALUE;
		public var xRegion:int = int.MIN_VALUE;
		
	}

}